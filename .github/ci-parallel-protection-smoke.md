# Temporary CI protection verification

This short-lived branch verifies that the aggregate CI check remains the only
required GitHub branch-protection status after the test suite is split into
parallel jobs. It will be closed and deleted after validation.