# Agent Instructions

Read @docs/shared-agent-instructions.md first — the durable source for
cross-interface agent behavior shared by every hat (Replit Agent, Claude
Code, Gemini, HolaHola runtime agents).

Then read @replit.md for this project's architecture, operating commands,
and safety constraints. Do not copy those instructions into this file: the
import structure prevents per-tool instructions from drifting apart.
