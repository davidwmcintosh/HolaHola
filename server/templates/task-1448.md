# Task 1448 — bounded Antigravity coding task

## Objective
Extend the existing `runtime and profile own authority while renewed credentials remain valid` test in
`server/scripts/test-coordination-runtime.test.ts`. Add assertions proving that a principal with the
same runtime registration and credential but a different profile cannot record an outcome receipt or
claim execution authority. Both attempts must fail with `consumption_not_authorized`. Do not modify
production code.

## Starting state
- Branch: `luca/gemini-experiment`
- Starting commit: `__FINAL_STARTING_COMMIT__`

## Bounds
- Change at most one file and no more than 40 KiB of patch.
- Wall-clock limit: 10 minutes.
- Maximum four logical model turns and eight API attempts.
- The only declared write target is `server/scripts/test-coordination-runtime.test.ts`.
- Allowed read-only commands: `git status`, `git diff`, `git log`, `git show`, and the exact focused test command.
- Focused test command: `npx tsx server/scripts/test-coordination-runtime.test.ts`

## Prohibitions
Do not commit, publish, deploy, mutate shared databases, access secrets, or use undeclared network access. Do not write outside the declared target.

## Independent verification
An independent cross-hat verifier must inspect the assignment, starting commit, path bounds, patch size, and focused test result. The verifier must reject any secret, undeclared path, command, or network use.