Credentials are injected. Testing a live exchange with a read-only inbox call:

Ran 4 commands

Both rejected. Checking whether the injected values are malformed (length/whitespace/quotes only, no values printed):

Inspected credential format without revealing values

The new credentials reached this session, but production rejects both of them, so this session has no working coordination access yet.

What's here:

COORDINATION_ACTOR=luca-claude-code
COORDINATION_RUNTIME_ID=luca-claude-code-cloud-2026-09
COORDINATION_API_URL=https://getholahola.com
COORDINATION_RUNTIME_BOOTSTRAP_TOKEN: 46 characters, starts with cb_, no extra spaces or quotes.
COORDINATION_LUCA_CLAUDE_CODE_TOKEN (the old per-agent token): 64 characters, no extra spaces or quotes.

What happens when they're used:

Swapping the bootstrap token for an access token (POST /api/coordination/credentials/exchange) returns 401 "Runtime bootstrap authentication failed".
Reading notes with the old token (GET /api/agent/notes?to=luca-claude-code) also returns 401.
The server itself is up: /api/health returns 200.

The values look well-formed, so the server most likely doesn't recognise them. The usual causes:

A rotation that wasn't finished. If the bootstrap came from coordination-runtime-rotation.ts stage, the stored hash may still be the old one until complete runs. Or the value pasted into this environment may be from an earlier rotation than the one production now holds.
The runtime ID doesn't match the registration. The bootstrap has to belong to luca-claude-code-cloud-2026-09 exactly. If it was issued under a different runtime ID, it will fail.
The old token was revoked. That's expected after migrating, but it means there's no fallback.

To fix: from the trusted server environment (Replit), check that registration's status. Then either finish the staged rotation, or issue a new bootstrap with coordination-runtime-bootstrap.ts and paste that exact value into this environment's secrets. Start a new session after that.

I only tried the exchange once, because that endpoint is rate-limited. I also couldn't use coordination-cli.ts: it needs NEON_SHARED_DATABASE_URL, which isn't set here, so I called the HTTP endpoint directly. No code changes,